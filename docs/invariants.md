# Invariant register

Every rule the design must always satisfy, in one place. Before this file existed
they lived in four documents with a hand-allocated numbering scheme that had
collided once, and the current text of some entries required reading two of them.

**The rule that keeps it true:** an invariant stated in a decision's amendments
is added here in the same commit that adopts the decision. A decision may state
an invariant; it does not own the numbering. Where a decision's text and this
register disagree, this register wins.

**What this becomes.** Today this file is written by hand and the numbers are
allocated here. That is a stopgap and it is the thing that already failed once.
Once the backend exists, each entry is a test carrying its own metadata, this
file is generated from the suite, and CI fails when the generated file differs
from the committed one. The identifier is then a constant in code, so allocating
the same number twice stops being a mistake anyone can make and becomes a
compile error. That is the same bidirectional-diff mechanism D25 already uses for
`@projection` columns, applied to itself.

Each entry carries a `status`. `specified` means stated and reasoned about with
no test behind it; `implemented` means something runs. The count of `specified`
entries is a number that can only go down, and it is a more honest measure of
progress than the count of decisions.

**Both counts are derived by the suite rather than typed**, which they were not
until the server arrived and proved why they should be: `implemented` read 0
through the whole of the backend being built, while a hundred and two checks ran
on every suite invocation. That is the sixth number in this family to drift and
the first caught by a test rather than by whoever next opened the file. The
derivation is `Check::Pending` against everything else, which is the same
distinction the status vocabulary makes.

---

## Classes

**Structural (S)** is checked against `information_schema` and `pg_catalog` in
CI. It needs no data. A failure blocks a deploy.

**Job-asserted (J)** is checked by the rebuild-and-assert cycle against real
data. A failure raises a `discrepancy`, never an error, so the model's
self-consistency lands in the same queue as every other finding and never stops
the floor.

## The vacuity column

Marked entries assert an **absence**: zero rows match, no column is named this,
no such thing exists. Every one of them **passes when its population is empty**,
so truncating a source, dropping a partition or simply not having built the
feature yet makes the check quietly start succeeding.

Fold invariants do not have this property. Delete half of `stock_movement` and
J1 fails, because the projection stops matching its source. That asymmetry was
first noticed for one case, the SSCC reuse guard, and it is general: **an
anti-join is only as strong as the assertion that its population is non-empty and
reaches back far enough.**

Marked entries need a companion population assertion. S34 is the general one for
history depth. The rest are named per entry as they are implemented, and an entry
marked here that reaches `implemented` without one is a lie the suite tells
itself.

**Vacuity is broader than this column, which the suite found on its first run.**
*(2026-08-04.)* The column marks entries that assert an absence, because an
anti-join over an empty table passes. That is true and it is the special case.
**Any universally quantified check proves nothing over an empty population**, so
"every RLS-protected table forces RLS" passes exactly as emptily when there are
no RLS tables as "no table has a `jsonb` column" does when there are no tables.
S7 and S20 carry no mark here and were both vacuous the first time they ran.

So the runner takes the verdict from **how many objects a check examined** rather
than from how the entry is phrased, and reports `VACUOUS` separately from `PASS`.
This column stays as a claim about the invariant; the count is the fact about the
run. Of the eleven entries implemented so far, five examined something and six
examined nothing.

---

## Structural

| # | Invariant | Owner | Assertion | Vacuity |
|---|---|---|---|---|
| S1 | Every `stock` key column except `tenant_id` and `item_id` appears on `stock_movement` as a `from_`/`to_` pair, under an explicit name map | D4, D12, D20, D24 | Catalogue diff. **Caught D20-broke-D12 and the undetected `lot_id` twin** | |
| S2 | Every table naming a stock cell carries **the complete column set**. The "or FK to `stock.id`" disjunction is removed: under a reapable `stock` the two are not equivalent | D24, Q91 | Catalogue diff over a declared list | |
| S3 | Every demand/cause CHECK on a grouping table is `<= 1`, never `= 1` | D10 corrected, D16 | Grep `pg_constraint`. **Caught D16-repeats-D10** in review, and was itself wrong for the whole time it had nothing to examine: the test was a substring match for `= 1`, which `<= 1` contains, so the first two constraints it ever saw were both flagged for being correct. **A vacuous check cannot be wrong out loud.** Corrected by D61 | ● |
| S4 | Every table registers exactly one provenance and one role, and the registry covers `information_schema.tables`, sourced from code and from `record_scheme` | D21, D26 | Bidirectional diff | |
| S5 | The **three-way** diff: every `@projection` column is registered, every registry row names a live column and a live function, and every `projection_%_rebuild` function has at least one registry row. **The third leg is the one that catches anything**: `order`'s four columns were neither commented nor registered, so the two-way diff agreed by both sides being empty, and only the function was evidence. `@projection(pending)` marks a column protected from the app whose maintainer is not written yet | D25, D49 | |
| S6 | Every fact table has no UPDATE and no DELETE granted to the app role | D25 | `role_table_grants`; attempt both, expect failure | |
| S7 | Every trigger maps to a registered projection. No trigger implements rules, validation, defaults or cascades | D25 | `pg_trigger` diff | |
| S8 | Every RLS-protected table with a `SECURITY DEFINER` maintainer has `FORCE ROW LEVEL SECURITY` | D18, D25 | `pg_class.relforcerowsecurity` | |
| S9 | Exactly three RLS shapes exist, selected by category. **Widened by D55**: the shared-reference shape is a read/write pair, so there are three permitted expressions rather than two. It reads `polqual` and therefore still sees only the read side, which is what let the write hole stand — S46 is the other half | D19 amended, D55 | `pg_policy` | |
| S10 | The model contains **no `jsonb` column** | Principle 3 | `information_schema.columns` | ● |
| S11 | No `policy%`/`%_policy` column has a name in {field, attribute, column, operator, comparator, expression, condition, rule, action, target, sql, script}; the only text columns are `policy_binding.note`, `policy_change.reason`, taxonomy `code`/`name` | D22 | Column-name denylist. **This is D13 made greppable, and it is the check the first draft failed, on `band_axis`** | ● |
| S12 | `policy_binding` has **no** `num_nonnulls` CHECK; its uniqueness is `NULLS NOT DISTINCT` and non-deferrable | D22 | Assert absence and presence | |
| S13 | The `policy_kind` enum, the `%_policy` table set and the Rust `PolicyKind` registry are the same set | D22 | Three-way diff | |
| S14 | Every `%_policy` table has `CHECK (kind = ...)`, the composite FK to `(policy_binding.id, kind)`, and the effective-range exclusion constraint | D22 | Catalogue scan, generated from one template | |
| S15 | Tenancy is index 0 of every kind's `DIMENSIONS` const, and every ordering is a permutation of all six dimensions. **Without the first, a platform-shipped default outranks a tenant's own configuration** — D22 calls that a correctness hole rather than a support surface. Without the second, some pairs of bindings are unorderable and the failure presents as a tie rather than as a missing declaration. Ran for the first time under D81, which built the registry it had been pending on since D22 | D22, D81 | Compiled registry check | |
| S16 | Every consuming column is named `<kind>_policy_id` and is an FK to the value table | D22 | Enumerated register + catalogue | |
| S17 | Every foreign key from a non-assertion table to an assertion table is **nullable**, excluding the assertion mechanism's own tables | D21 | `pg_constraint` ⋈ `attnotnull`. *Scoped: the first draft's version failed on its own `assertion_stance`* | ● |
| S18 | No table in the assertion set has a `status` or `state` column | D21, D25 | Column-name check | ● |
| S19 | Every fact row has a `client_event` FK, and `fact.recorded_by_id` = `client_event.recorded_by_id` where the person arm is set | D5, D11, D25 | Catalogue + anti-join | ● |
| S20 | Every generated `ext_*` table matches its scheme's compiled fields, its `manifest_hash` recomputes, its parent FK is `ON DELETE RESTRICT`, and it has RLS with FORCE | D26 | Drift job | |
| S21 | Every application metric-code literal appears in the reserved seed with `reserved = true, tenant_id IS NULL` | D23 | Grep | ● |
| S22 | No canonical unit has a non-zero offset or a factor other than 1/1 | Principle 5 | Two count-zero queries | ● |
| S23 | No resolver call appears inside a loop | Principle 6, D22 | AST check | ● |
| S24 | `expected_supply` has one partial unique index per provenance arm, and **no** unique index over `(item_id, owner_id, status_id)`. The per-arm index is the idempotency guard that makes reprocessing a supplier message safe; the forbidden one would collapse two purchase orders promising the same goods to the same place into one promise. **The arm set is read from the catalogue**, so this widens by itself as the transfer, asserted and return arms arrive | D24 supply side | Catalogue scan | |
| S25 | Availability indexes on `stock` and `expected_supply` both carry `owner_id` and `status_id` **in the key**, not in an INCLUDE payload, which cannot be searched on. Otherwise the availability read joins `inventory_status` to find out whether a row counts, and a join in that path is the cost D24 built this shape to avoid | D24 supply side | Catalogue + query register | |
| S26 | `expected_supply` carries at most five maintained quantity columns; a sixth requires a recorded decision. **The cap is the point rather than the number**: this row folds intentions, assertions and facts, and every additional maintained quantity is another thing that can disagree with the others. Generated columns are excluded because they are the arithmetic rather than another input to it | D24 supply side | Catalogue count | |
| S27 | The outbox source reference is **never dereferenced**. The rendered bytes in `party_message_id` are the payload | D26 | Query-register grep | ● |
| S28 | Exactly one foreign key in the schema targets `stock(id)`, and it is `stock_allocation.stock_id`, declared `ON DELETE RESTRICT`. `stock_allocation(stock_id)` has a plain btree | D24, Q91 | `pg_constraint` ⋈ declared list; catalogue scan | |
| S29 | `stock` and `package_content` carry neither the attachable nor the subscribable capability flag in the code-side table registry; `projection_check.scope_kind` may not name a stock cell | D26, D25, Q91 | Registry check + enumerated denylist | ● |
| S30 | Projection tables have no **DELETE** granted to the app role. S5 covers UPDATE; S6 covers fact tables | D25, Q91 | `role_table_grants`; attempt, expect failure | |
| S31 | Every identification datum on `activity_event` is a typed column. `activity_event.detail` appears in no `WHERE`, `GROUP BY` or `JOIN` in the query register | Principle 3, Q89 | Query-register grep | ● |
| S32 | `activity_event` is range-partitioned on `occurred_at` with local indexes from the first migration, and carries `tenant_id NOT NULL` and `site_id` | D18/J20, D25, Q89 | Catalogue scan | |
| S33 | No `party.gs1_company_prefix` is all-zero or absent-yet-used; no literal default prefix appears in code; SSCC issuance is gated on an explicit `number_range` row | D20, Q90 | CHECK + grep | ● |
| S34 | For every row in `retention_floor`, either the oldest live partition or the archive index covers back to `now() - minimum_age`. **This is the companion assertion for every vacuity-marked entry whose population is bounded by history depth** | D31 | Partition catalogue + archive index | |
| S35 | Every `asserted_unit` node read as **non-physical** carries a NULL `sscc` and resolves to no package. The 856's `S` and `O` levels are documents while `T` and `P` are pallets and cartons, and D43 stores the order level as a node for that reason, so a declared tree contains rows that are not things. **Pending for eleven migrations on the grounds that the collapse at receipt did not exist**, which was true of the code and hid that the column the sentence needed was missing too: `resolved_physical` is our reading of their raw level vocabulary, and D96 added it. Built as **two CHECKs rather than a job**, which is stronger than the register asked for — the rows cannot be written rather than being found afterwards. The check therefore asserts the constraints are present, in S12’s idiom, because a dropped CHECK leaves no violating row behind to find | D43, built by D96 | Both CHECKs present in `pg_constraint`, plus the property over every node whose level has been read | ● |
| S36 | No table in the receipt set carries a stored received quantity, variance or accumulator column. Received is a fold over `stock_movement`, so the number cannot disagree with the ledger that produces it | D45 | Column-name denylist over a declared table set | ● |
| S37 | `location.zone_id` is a **composite** foreign key including `site_id`, and `zone` carries the matching `UNIQUE (id, site_id)`. Replacing it with a simple FK lets a location sit in another site's zone, and every zone-scoped policy resolution for that location then returns another site's answer with nothing complaining | D46 | `pg_constraint` | |
| S38 | `stock_movement.reverses_movement_id` is a self-referencing FK, and the two in-row correction CHECKs are present. **The four cross-row rules are deliberately not here**: D47 tried them as a trigger, S7 rejected it on D25's grounds, and they moved to the application write path with J50 as the backstop | D8, D47 | |
| S39 | `intention_amendment.revision_class` is `NOT NULL`, carries **no default**, and is of the shared `revision_class` enum; the correction-reason CHECK is present. **The absent default is the point**: restoring it is one line and makes every unthinking correction a `world_event`, which sorts by the moment it was typed rather than the moment it is about | D48 | |
| S40 | No table in the order set carries a stored line total, extended amount or tax column. Same shape as S36: an extended amount is quantity times price, so storing one creates a number that can disagree with its own inputs. **Tax is on the list for a second reason**: `unit_price_minor` is tax-exclusive by decision, and that erodes the moment a `tax_minor` column appears beside it and makes the first column's exclusivity ambiguous to anyone reading the schema | D40, D50 | ● |
| S41 | Every `new_%` column on `intention_amendment` is named by **both** the subject CHECK and the changes-something CHECK, and `intention_amendment_line_fk` is composite through `order_id`. **The covered set is read from the catalogue rather than listed here**, because the way this breaks is not a dropped constraint: it is a ninth covered column added to the table and left out of the CHECK bodies, which is exactly how the changes-something CHECK came to name four columns on a table that had four and then had eight. The composite FK is S37's rule one level over: without it an amendment names order A and a line belonging to order C, the fold writes C's line, and every report about A reads consistent | D42, D51 | |
| S42 | Every database object a pending check names as its blocker is **actually absent**, across both classes. D49 named the failure — a check whose stated precondition has since been met, still doing nothing, still reading as deliberate — and by migration 14 fourteen entries were waiting on things already built. **Every named object must be absent, not merely one**: S33 waited on "party and number_range" long after `party` arrived. Blockers naming what SQL cannot see (`Blocker::Elsewhere`: a code registry, an AST pass, an open question) are counted and reported rather than checked, so the unverifiable set is a number rather than a silence | D49, D52 | |
| S43 | Every `projection_rebuild` row names a function **family** whose body actually writes the column it claims to maintain. **S5's fourth leg**: its three all pass on `stock.allocated_quantity`, which was registered to `projection_stock_rebuild` from migration 3 and never written by it. Table, column and function all existed, so every existence check agreed while the column kept whatever it was last set to. The unit is the family — every `projection_<subject>%` function — because S5 already records that steps like `projection_package_stamp` are registered under the rebuild they belong to. **Greppable, with S11's limit**: a maintainer writing through dynamic SQL would pass while doing nothing | D25, D53 | |
| S44 | No table in the fulfilment set carries a stored `progress`, completion or rollup status column. D25 dropped `order.fulfilment_status` as a third hop and said an order has few fulfilments, compute it on read; **the same argument applies one level down to `fulfilment.progress` and D25 did not apply it there.** A fulfilment has few lines, and any label it could hold is a function of the four coverage quantities that can disagree with them. Same shape as S36 and S40 | D15, D25, D53 | ● |
| S45 | On every table the application may write, each non-projection column appears in one of its grant lists. **Every other guard here watches for too much privilege** — S6 and S30 for facts and projections, J36 for `@projection` columns — and a column writable by nothing is the opposite mistake, just as silent, because everything that could notice is pointed the other way. `order.currency` was added by D50 and granted to nobody, so the application could price a line and never record the currency J54 requires it to have. **Two drafts got the rule wrong before this one**: "any role" missed it, because `nylonite_projection_owner` holds table-wide UPDATE on `order`; "`nylonite_app`" reported thirty-eight columns the application is deliberately forbidden. It breaks the same way every time: a column added to a table whose grants were already enumerated | D25, D54 | ● |
| S46 | Every shared-reference table pairs a `FOR SELECT` read policy with a write policy declaring an **explicit `WITH CHECK`**. **A policy with no `WITH CHECK` inherits its `USING`**, so "readable when shared or ours" silently meant "writable when shared or ours", and any tenant could mint or edit rows every other tenant reads — including `item`, `metric` and `policy_binding`. Three legs, one per way the pair rots: the read half is `FOR SELECT` or it ORs itself back into the write path; it never appears without its write half; the write half states its check rather than inheriting one. **What it cannot do is prove the predicate is right**, only that the pair is shaped as declared; the behaviour is verified by negative control | D18, D19, D55 | |
| S47 | No column defaults to `gen_random_uuid()`. Identifiers are time-ordered, so inserts append at the index's right edge rather than scattering across it. **Asserts the absence rather than the presence of `uuidv7()`**, deliberately: nothing reverts a column that already has it, and what does happen is a new table written from the pattern of the forty-three before it. Phrased the other way it would also be wrong, since a column may legitimately have no default | D57 | ● |
| S48 | No table in the movement set carries a **landed cost, valuation or cost-tax** column. S40's boundary on the other side of it. A landed cost is freight and duty apportioned across lines — a computation over facts living elsewhere, so a stored one disagrees with its own inputs the moment a freight invoice is corrected. A valuation depends on a costing method nobody has chosen and D40 puts it outside this system. `consignment.price_minor` is out of scope for S40's reason: a carrier's quoted charge is a number they gave us | D40, D58 | ● |
| S49 | Every `projection_%` function is a **declared step** exactly once, and every declared step names a live function. **S5's third leg over the wider set**: S5 reads `projection_%_rebuild` and is right to, but it cannot see a step — `projection_package_stamp` and `projection_stock_resolve_locations` match no such pattern, own no column, and exist to finish what another function started, so S43 passes throughout because the *family* writes the column. Measured rather than argued: running everything in `projection_rebuild` leaves nought of two packages stamped, and the declared run stamps both. **Orchestrators and request helpers are not steps** (`projection_run_all`, and under D107 also `projection_run_dirty`, `projection_refresh_tenant`, `projection_mark_dirty`); an orchestrator that ran itself would not terminate. Listed rather than pattern-matched so a new real maintainer still has to appear in `projection_step`. **Scoped to VOLATILE functions by D95**, because the name prefix is not the property being asserted: a maintainer writes, and D95's `projection_age` reads. `provolatile = 'v'` rather than a name exception, because Postgres enforces it — a STABLE function cannot write and so cannot be a maintainer whatever it is called, which covers the next reader in this family without anyone remembering to add it. S17's scope is written down for the same reason. | D35, D64, D107 | |
| S50 | Every table a resolution reads — the scope, the value tables, the closures — reaches `policy_epoch` by some route: `policy_change` for the values, `policy_binding` for the scope, `projection_step` for the closures. **An input the epoch cannot see is a cached answer that never goes stale**, which is question 80's own statement of the failure: the floor runs on stale weights and nothing detects it. Read from the catalogue rather than listed, so a fourth `%_policy` table is covered the day it exists — seven of D22's ten named kinds still have none. **The effective-range half is deliberately absent**: no epoch can see a range opening or closing, because nothing is written when it does | D22, D70 | |
| S51 | Every foreign key to a **strictly tenant-owned** table (`tenant_id NOT NULL`) carries that table's `tenant_id`, and every such table offers the `(id, tenant_id)` key to carry it. **RLS filters reads and says nothing about what a row may point at** — D55 found that hole open since migration 1 and closed it by hand. Shared-reference tables are excluded because a platform row has no tenant to match, which makes the composite form impossible rather than merely absent; that half is J64. Two constraints are exempt by name, both tenant-determined through a column another invariant requires: `location_zone_fk` via S37, `intention_amendment_line_fk` via S41 | D55, D79 | Catalogue scan over `pg_constraint` | ● |
| S52 | Every `%_policy` value table is named by **both** `policy_candidate` and `policy_value`. A kind whose table neither function knows **resolves to nothing at all** — not to an error, not to a default, but to "no policy applies", which is a legitimate answer the caller cannot tell apart from a missing table. Migration 38 predicted this coupling and misattributed the guard to S50, which is about what the *epoch* sees rather than what the *resolver* sees; D85 walked into the gap within the hour, and then into the same list hardcoded a third time inside J13 | D82, D85 | `pg_get_functiondef` against the catalogue | ● |
| S53 | No `SECURITY DEFINER` function is owned by a **SUPERUSER or BYPASSRLS** role. A definer runs its body as its owner, and such a role bypasses row-level security unconditionally — so a definer owned by one is a hole in the tenancy boundary, and a definer the application is granted EXECUTE on is that hole handed over deliberately. **This is the check that decides how question 161 gets fixed rather than a check on the fix**: the naive answer to "the function has no privilege" is `SECURITY DEFINER` owned by `postgres`, which would have reopened what D55 spent a migration closing. The safe shape predates the check — fifteen projection definers owned by `nylonite_projection_owner`, which is neither, against tables forcing RLS so it applies to the owner too | D18, D55, D94 | ● |
| S54 | Every `mediated_write` column: the app holds **no** UPDATE, the mediation owner **does**, the named function exists, and it is a `SECURITY DEFINER` owned by that role. Four properties because there are four ways to get it wrong and **two of them were live**: D90's function could not run for want of privilege, and D89's app kept UPDATE on the disposition columns so its function was a courtesy anyone could decline. Both were invisible to the whole suite, because a convention nobody writes down is a convention nothing can test — which is why the registry table exists at all, on `projection_rebuild`'s pattern and for `projection_rebuild`'s reason | D89, D90, D94 | |
| S55 | Every projected column is written **only by its registered family**, and a column with more than one writer is on the **declared multi-writer allowlist** with exactly those co-owners. **S43's converse**: S43 checks that a registered family writes the column it claims; nothing checked that nobody else does. D101 was two maintainers of the same `projection_stock%` family owning `resolved_location_id` between them for fifty-two migrations, invisible because values were always right and S43's unit is the family. The catalogue reads SET / DO UPDATE assignments table-scoped (so a SELECT of the same name is not a write), requires every writer in the family, and requires any multi-writer column to be allowlisted — today only `stock.resolved_location_id` and `stock.site_id`, step 30 for the location-held arm and step 70 for the package-held arm. A dual write of `stock.quantity`, or a third writer of either, fails without waiting for a fixture shape. Greppable, with S11's limit | D101, D104 | |
| S56 | Every VOLATILE `projection_%` function body carries a **last-changed marker** naming the migration that last replaced it: `-- last changed: migration NNN (Dxxx)`. **A `CREATE OR REPLACE` carries no record of what it replaced**, and the decision record names the migration that introduced a function rather than the one that last changed it — migration 56 was first written against migration 21's body and nearly reverted D65 and D68. The marker is a body comment, not `COMMENT ON FUNCTION`, so pasting an older body keeps the older marker and an intentional edit bumps it. Scoped to VOLATILE for S49's reason: a maintainer writes, and `projection_age` only reads. The orchestrator is included | D104 | |

## Job-asserted

| # | Invariant | Owner | Vacuity |
|---|---|---|---|
| J1 | `stock.quantity` = the signed two-sided fold of `stock_movement` over the cell key | D5, D12, D24 | |
| J2 | `stock.weight_g` = the same fold over `catch_weight_g` | D20 | |
| J3 | `stock.allocated_quantity` = active cell-bound allocations only, enumerating `{allocated, picking, picked, packed}`. **It is a quantity fold and must never be used as a reference test**: terminal allocations hold `stock_id` and contribute nothing to it | D12 narrowed, Q91 | |
| J4 | `expected_supply.quantity_allocated` = active allocations against it. **J3's fold on the supply side of the same allocation**: an allocation names a stock cell or an expected supply and never both, so J3 and J4 partition one table between them, on the same state set because a despatched or released allocation has stopped claiming whichever side it claimed from | D24 | |
| J5 | `stock.resolved_location_id` = holder location, or the holder package's | D24 | |
| J6 | `package.parent`, `location`, `resolved_location`, `status`, `depth`, **`sscc`, `barcode` and `identifier_kind`** = the fold of `package_event` in `(occurred_at, recorded_at, id)` order; replay in **any** arrival order is identical | D24, Q90 | |
| J7 | `stock.quantity > 0 ⇒ resolved_location_id IS NOT NULL` → `stock_without_location` | D24 | ● |
| J8 | `expected_supply.quantity_refined` = the sum of refining rows, the double-count guard | D24 | |
| J9 | No active allocation references a closed `expected_supply` → `supply_withdrawn`. **The allocation is not released**, and D24 is explicit that a counterparty's retraction must not silently un-promise a customer order. A human decides, which is D8's thesis applied to supply rather than to stock | D24 | ● |
| J10 | `observation_current` is exactly rebuildable from `observation` **plus the recorded precedence policy row**, which D86 restored: the column S16 and S43 rejected under D78 for naming a table that did not exist now names one and is written by the maintainer. Each row is recomputed under the policy *that row records*, NULL meaning the default rule — without which the first per-metric run would report the policy working as drift, which is precisely the failure D23's general rule describes | D23, D78, D86 | |
| J11 | A counterparty-asserted observation enters `observation_current` only with an `acceptance` | D23, D25 | ● |
| J12 | `package` dimensions equal `observation_current` **for unsealed packages only** | D23 | |
| J13 | Every **tenant** policy value version has a `policy_change` at the moment it came into force, anti-joined both ways. The match is on `lower(effective) = occurred_at` rather than merely on the binding, because a binding revalued four times carrying one change record satisfies a binding-level join and is the case worth catching. `retired` closes a range rather than opening one and is excluded from the second direction. **Platform-shipped versions are exempt, and that is a limit rather than a decision**: `policy_change` reaches `client_event` through `(tenant_scope_id, client_event_id)` and `client_event.tenant_id` is NOT NULL, so an act belonging to no tenant has no event to name and a shipped default cannot carry provenance at all. Question 138 | D22 | |
| J14 | Every scope FK **and every value-table FK** resolves within the binding's tenant; a platform-shipped binding references only shared rows. **The second clause is the one with teeth, and it caught a live instance on its first run**: `policy_binding` is under the shared-reference RLS shape, so a platform row with `tenant_id IS NULL` is readable by every tenant, and scoping one to a single tenant's item makes a shipped default that is not a default. The population is every scope axis actually named, not every binding, because an all-NULL platform default has nothing to resolve | D22, D18 | ● |
| J15 | No binding names a node inconsistent with a coarser node on the same dimension: item ∉ item_class, zone ∉ site, party ∉ party_class. **Not catchable by a foreign key**, because each one is individually valid; the symptom is a policy somebody configured that silently never matches, since the resolver walks ancestor-or-self on both axes and no row satisfies both. Owner is a party with no class axis beside it, so it pairs against `party_class` too | D22 | ● |
| J16 | Zero `discrepancy` rows of kind `policy_ambiguous` after the suite. Two bindings at equal specificity is the one outcome most-specific-wins cannot decide, and D22 chose to raise it rather than pick arbitrarily. The population is every discrepancy rather than every ambiguous one, so a clean queue reports as examined rather than as vacuous | D22 | ● |
| J17 | `assertion.supersedes` is acyclic; at most one in-force assertion per `(tenant, author, kind, author_reference)` | D21 | ● |
| J18 | A counterparty observation's promoted value still equals its assertion's | D21, D23 | |
| J19 | Truncate every assertion table, rebuild `stock` and `allocated_quantity`, assert byte-identical, **and** assert that no column of the `stock` key is reachable from an assertion table by any path that survives truncation. `expected_supply` and `stock_allocation.expected_supply_id` are exempt and named | D21 rule 3, Q90, D24 supply side | |
| J20 | Every `tenant_id` on a fact agrees with its subject's and its event's | D18 | ● |
| J21 | Per-tenant extension ceilings hold. **Superseded as a job by D36**: the ceiling is now claimed slots enforced at declaration, so this becomes the assertion that no tenant holds more claimed slots than issued | D26, D36 | ● |
| J22 | Golden snapshot: no recorded resolution changes without an explicit `RESOLVER_VERSION` bump. **The only check in this suite that watches meaning rather than structure** — the inputs it protects are D81's orderings, D83's clamp directions, the specificity constants and the taxonomy itself, none of which changes a table definition. Measured: a re-parent under D72, entirely legitimate and with no code change, silently removed a hundred-and-twenty-day shelf-life floor and every structural check still passed. A stale snapshot after a bump is refused rather than accepted, because the bump exists to make somebody read the diff | D22, D84 | ● |
| J23 | The five ingestion channels produce identical content columns | D23 | |
| J24 | Projection cascades are at most two hops from the originating fact | D25 | |
| J25 | `refines_expected_supply_id` is acyclic and of depth exactly 1; violations raise `refinement_too_deep`. **Asserted by a job, never a CHECK**: a CHECK on a projection column wedges the rebuild | D24 supply side | ● |
| J26 *(corrected)* | `expected_supply.quantity_received` = the fold of `stock_movement` rows whose `goods_receipt_line_id` names this supply row or any row refining it. **The original folded `goods_receipt_line` rows, which carry no quantity**: D45 defined the table and found the invariant naming the right relationship over the wrong one, so it could not have run. **Corrected by D102**: an arrival counts for what is left of it, so the fold subtracts corrections against each movement. It read 100 against a receipt where 96 arrived from the migration that built it, and agreed with its maintainer the whole time, which is what a check duplicating its maintainer's SQL is worth when both are wrong the same way. **D103** reads it through `stock_movement_effective`, so the netting follows a correction chain of any depth rather than stopping at the first level | D24 supply side, D45, D102, D103 | |
| J27 | Transfer arm: **no unit is simultaneously counted** in origin `stock.available_quantity` and destination `quantity_promisable` | D24 supply side | ● |
| J28 | No open row with `quantity_outstanding > 0` past `expected_to + grace` → `supply_overdue` | D24 supply side | ● |
| J29 | No active allocation references a closed or overdue row → `supply_withdrawn` / `commitment_unbacked`. The allocation is **not** released by the job | D24 supply side | ● |
| J30 | Rebuilding `expected_supply` preserves row identity. Truncate-and-regenerate is forbidden while any allocation holds an `expected_supply_id`, which is `ON DELETE RESTRICT`, so regenerating would either fail outright or orphan a commitment. The maintainer upserts on the arm's partial unique index instead. **The stronger half is a property test** beside J6's and J46's: rebuild three times and every id is the one it was | D24 supply side | |
| J31 *(narrowed by D99)* | `fulfilment_line.covered_quantity` equals the fold of `stock_allocation` over that line, on the covering state set — `allocated, picking, picked, packed, fulfilled`, with `short` and `released` in none. **Widened by D53 to four quantities and narrowed by D99 back to one.** J3's shape one level over, and the two deliberately differ: on the cell a despatched allocation must not count because the stock has left, on the commitment it must because it is the most covered a line can be. **This check was written from J3's state set one commit before D53**, because both columns were called `allocated_quantity`, which is what the rename to `covered_quantity` exists to stop. D99 then separated the four by what they are evidence *of*: coverage is a question about intentions and D12 makes an allocation exactly that, so this fold is right and stays; the three progress quantities assert what physically happened and moved to J68. Keeping all four here would have left one source answering two kinds of question, which is D53's own catch one level further on | D24 supply side, widened by D53, narrowed by D99 | |
| J32 | The reap predicate is the complement of the rebuild's existence predicate: reap, rebuild, assert produces zero `projection_drift`. **Structural since D35**, because the reap is a phase inside the rebuild function and the intermediate state is unobservable | D24, D25, Q91, D35 | |
| J33 | Rebuilding `stock` preserves row identity: every live cell resolves to the same `id` before and after. Truncate-and-regenerate is forbidden while any allocation holds a `stock_id`. *(J30's missing analogue.)* | D24, Q91 | |
| J34 | No `package` row's earliest `package_event` has `source = 'asn'`. Minting from an assertion is structurally absent | D21, D24, Q90 | ● |
| J35 | Every `sscc_allocation` serial lies within its `number_range`'s issued span, and no serial is reissued within the reuse window, 12 months plus any sector extension. **The exemplar of the vacuity problem**: truncate the history and it starts passing | D21, Q90 | ● (S34) |
| J36 | No role other than the projection maintainer may `UPDATE` any column commented `@projection`, or `DELETE` from the table carrying one. **Rewritten by D49 after it was proved unable to fire**: it read `information_schema.role_table_grants`, which shows only grants the current user is party to and only table-level ones, so a column-level `GRANT UPDATE (quantity) ON stock` never appeared in it. Now `has_column_privilege`. `INSERT` is deliberately excluded: the fold coalesces onto the row's own value, so the original must be writable once | D35, rewritten by D49 | ● |
| J37 | Every `SECURITY DEFINER` function in the maintainer set has `search_path` set in `proconfig` and no `EXECUTE` grant to `PUBLIC`. **Catches a new maintainer function written correctly and deployed with Postgres defaults**, which is the likely failure rather than a deliberate one | D35 | ● |
| J38 | `relforcerowsecurity` is true on every table carrying an `@projection` column | D35 | |
| J39 | Every `record_scheme` row has a claiming `extension_slot`, and no slot is claimed by two keys | D36 | |
| J40 | No `record_scheme` whose slot is released has an unarchived materialised table | D36 | ● |
| J41 | `expected_supply.inbound_shipment_id` equals the walk `asserted_unit_content → asserted_unit → assertion → despatch_advice` for every assertion-arm row, and is NULL for every other arm | D37 | |
| J42 | Every GTIN in `item_barcode` is 14 characters and passes the mod-10 check digit. Asserted over the table, not the call site, because normalisation happens on write and a second write path will be added | D34 | ● |
| J43 | The `item_barcode` exclusion constraint's `COALESCE` sentinels are present. The failure mode of their removal is that duplicate shared barcodes become insertable and nothing complains | D34 | |
| J44 | No externally-authoritative order carries a `world_event` amendment **made by a person here**. **Narrowed by D48, corrected by D54**: D48 read D39 as a class of change, but a counterparty cancelling their own order arrives on the channel, sets `order.state`, and is a `world_event` — forbidding it leaves something that unambiguously happened inexpressible. **What D39 forbids is a place, not a class**: their event reaching us through the channel is their amendment; a person here typing it is us editing their order. `intention_amendment_actor_ck` carries the distinction. **The proxy is imperfect and the imperfection is question 105**: `automation_key` is undefined, so a local job holding one passes a check a person fails. The first clause became structural in migration 16 — `source_channel_id` is NOT NULL with an FK, so no path can omit it | D39, narrowed by D48, corrected by D54 | ● |
| J45 | No query in the register reads across tenants except those on a declared exception list, and every entry on that list carries a cohort floor | D41 | ● |
| J46 | Every covered `order` **and `order_line`** column that an amendment sets equals the last writer over that subject in `(occurred_at, recorded_at, id)` order; replay in **any** arrival order is identical. Same shape as J6. **Widened by D51** to the line's four. **The clause "that an amendment sets" is a real limit, not hedging**: D42 makes the projection the original plus the amendments, and the original arrives on INSERT into the same column it seeds, so a column no amendment has touched has no independent source to be checked against. That the base is unrecoverable if the column is ever reset is question 137. **This sat `Pending("order and intention_amendment arrive with the outbound migration")` from before migration 9 delivered both until D51** | D42, widened by D51 | |
| J47 | No **physical** `asserted_unit` subtree, and no `package`, resolves to content lines naming more than one purchase order. D43 decided one `goods_receipt` per delivery per demand document, and both standards put the order level *above* the physical levels, so every pallet descends from exactly one order node — a pallet spanning two means the tree was built wrong rather than that a supplier did something unusual. **The scope is the part that took a decision to become sayable**: not every node, because a shipment node above two order nodes spans both and is correct, but every node read as physical, which is what D96's `resolved_physical` records. So this was blocked less on the collapse it named than on the reading of the author's level vocabulary underneath it. Only the resolved side — `raw_po_reference` is a string in the author's words, and two spellings of one order would read here as two, which is a finding about our parsing dressed as a finding about their pallet. **Demonstrated rather than assumed**: a test builds the violation, runs the real check against it, and asserts both arms fire, because a clean population proves only that a check does not fire on good data | D43, built by D96 | ● |
| J48 | Every outbound `party_message` on a channel whose `party_profile` requires acknowledgement has one, or a finding naming it. **Needs the companion assertion that outbound messages exist at all**: before the first EDI customer the population is empty and this passes without checking anything | D44 | ● |
| J49 | No `document_response` assertion names a subject assertion of the same `direction`. A counterparty's disposition is always of a claim travelling the other way, and the inverse is a mapping bug that would otherwise look like data | D44 | ● |
| J50 | Every correction carries its **target's** `occurred_at`, mirrors its target's sides, names the same item and the same tenant, and gives a `record_error` reason. **The `occurred_at` clause is the one with no symptom**: a correction stamped at the moment of discovery makes every as-at query answer with the pre-correction number forever | D8, D47 | ● |
| J51 | Corrections against a movement never exceed it in total quantity. **A finding rather than enforcement**: the trigger rejects a single over-large correction, but two concurrent ones each see a pre-image without the other, and the lock that would prevent it is the coordination D5 refuses. **Widened by D103 to the chain**: a movement is over-reversed exactly when what is left of it after its whole correction subtree goes negative, read from `stock_movement_effective`. Not a refactor — a correction of twenty-five itself corrected by ten takes back fifteen, and against a target of twenty that is legitimate, which the face-value form reported as overreach: a check firing on its own arithmetic rather than on the data | D47, widened by D103 | ● |
| J52 | `reverses_movement_id` is acyclic. **Not reachable by ordinary writing**: the FK is not deferrable so a target must already exist, and the app role has no UPDATE. It covers the maintainer role and a restore with FK triggers disabled, which is where a cycle would actually come from and when nobody is looking. Same shape as J17 | D47, J17 shape | ● |
| J53 | Every `record_error` amendment carries an `occurred_at` matching its order's `placed_at` or another amendment's `occurred_at` on the same order. A movement correction names its target by FK; an amendment has no target row, because the value it revises may be the order's own original, so the rule is expressed against the moments at which something was actually set | D48 | ● |
| J54 | No priced line on **either order kind** belongs to a header that names no currency. **Widened by D59**, because a purchase order is an order we place and D50's pairing is one rule over two tables rather than a second that happens to look alike. `record_scheme_field` states the pairing for one row, `currency` NOT NULL exactly when the field is `money_minor`; here the money is on the line and the currency on the order, because one order has one currency and a per-line column would only ever agree with itself, so no CHECK reaches both | D50, widened by D59 | ● |
| J55 | No `fulfilment` that is not cancelled commits to a **removed** `order_line`. D51 made removal expressible, and the state it makes reachable is the floor still committed to picking something the customer has withdrawn. **A finding rather than a constraint, and the ordering is why**: the customer removes the line at 09:00 while the picker is holding the carton, and a CHECK would refuse to record something that happened, which is the trade D5 exists to refuse. So the removal lands, the commitment stands, and the disagreement goes to the queue with both halves named. A cancelled fulfilment is excluded because it is the resolution rather than the problem | D51 | ● |
| J56 | No `fulfilment_line` is covered beyond its own quantity, and despatched ≤ packed ≤ picked ≤ covered. **`stock_allocation` has no constraint tying its sum to the line it serves**, so over-commitment is expressible; it drives `uncovered_quantity` negative, and the partial index on `uncovered_quantity > 0` then drops the line silently, which is right for the index and exactly wrong as a way to find out. Monotonicity holds by construction while the rebuild folds nested sets, which makes it cheap rather than pointless: it catches a hand-edited column, a restore, or a future state added to one set and not those above it. A finding rather than a CHECK, because over-commitment is a planning error somebody has to unwind and refusing the write would leave it unrecorded. **Load-bearing since D100**: the monotonicity held by construction while one fold produced all four from nested state sets over one table. D100 folds `covered_quantity` from `stock_allocation` and the other three from `stock_movement`, and nothing ties the sources together — a pick with no allocation, or an over-pick, now produces `picked > covered`. D12 permits both, and the allocation fold did not make them safe, it made them invisible by discarding a real pick no intention row carried | D53, load-bearing since D100 | ● |
| J57 | Every row naming an `item_packing_config` — `stock_movement`, and since D92 `goods_receipt_line`, and since D93 `asserted_unit_content` — names one **for its own item** that was **already effective** when it happened. **Widened rather than duplicated** each time: nothing in the rule was ever about movements specifically, and each table carries the identical triple with the identical exposure; the ledger was simply where D58 put the columns first. **Each table's moment is a different column and the differences carry meaning** — a movement has its own `occurred_at`, a receipt line takes its receipt's `received_at`, and a claim line takes its assertion's `asserted_at`, the author's clock, because the case pack that applied is the one in force when *they* packed. `received_at` stands in when they stated no time, which is D5's both-clocks rule. The claim arm tolerates an unresolved item: a GTIN we cannot place yet is D21's normal state, not evidence the config is wrong. D23 versions the config by `effective_from` so a corrected case pack cannot rewrite history, and D58 makes that load-bearing by recording which version converted the entered quantity — neither is worth anything if the recorded version is the wrong one. **The foreign key catches neither failure**: a config created next March is a valid row for a movement that happened last August, and a config for another item is equally valid as a reference while describing something else's carton size. A job rather than a CHECK because the comparison spans two rows and one of them can be edited afterwards | D23, D58, widened by D92 and D93 | ● |
| J58 | No `expected_supply` row carries claims it can no longer satisfy: `quantity_promisable` is never negative. D24 specifies that allocations are re-pointed from the promise to the new `stock_id` at receipt, in the same transaction as the movements; nothing performs that yet because it is the receiving path rather than the schema, so a fully received promise goes on carrying its claims. **The fixture produced exactly that on its first run**, which is where this came from. Two causes, one finding: the re-point did not happen, or more was promised than the row ever had. Not expressible as a CHECK, because the generated column derives from five maintained ones a rebuild sets independently | D24 supply side, D61 | ● |
| | **D62 note:** `crates/server/src/receiving.rs` now performs the handover this finding exists to catch the absence of, and raises the same condition at the dock rather than waiting for the job. J58 stays, because a row can still reach that state by a restore, a backfill, or a path nobody has written | | |
| J59 | `item_class` and `party_class` parentage is **acyclic**, and the closure carries a depth-zero row for every node. A two-node cycle is insertable — `%_not_own_parent_ck` forbids only self-parenting — and a naive recursive walk over one does not terminate. **D72 closed the direct route and kept the check**: no login role may UPDATE `parent_id` at all now, so a cycle takes two individually-legal `reparented` acts and arrives through the fold. Prevention would be a cross-row constraint, and the trigger that would carry it is forbidden by S7, so this stays a finding rather than becoming a constraint. The rebuild uses SQL's `CYCLE` clause so it degrades to a partial closure instead of hanging, **which makes the maintainer safe and the data still wrong**: the dropped branch is a subtree with no closure rows, so every policy scoped inside it silently stops matching. The self-row is the second half, because ancestor-or-self is one lookup and a node missing its own is invisible to a binding naming it directly | D22, D63 | ● |
| J60 | Every recorded re-parent carries the blast radius its approver was shown. `affected_binding_count` is frozen on the fact because **it cannot be recovered later** — it counts the bindings that were active against a taxonomy shape the move itself replaced, so a row without one is a decision nobody can audit. A check rather than a NOT NULL for two reasons: a move recorded before D73 legitimately has none, and backfilling it would manufacture evidence of a conversation that did not happen. **What it cannot check is whether the stored number is the one the function returned** — that needs the pre-move taxonomy, and S7 forbids the trigger that would catch it at write time | D22, D73 | ● |
| J61 | A retired class is not still being built on: no unretired child hangs under a retired parent, and no binding is created against a class already retired. **Neither is an outage** — D74 makes retirement a statement to the editor rather than the resolver, so both still resolve exactly as before. What they mean is that the retirement was not honoured and the taxonomy now says two things. **The third case is absent and cannot be added**: an item classified into a retired class needs a moment to compare against `retired_at`, and `item_classification` is a current-state table with no timestamp | D74 | ● |
| J62 | Every class records where it started. D72 made `parent_id` the fold of the acts and there was no act for the one that placed the class to begin with, so the fold's base was the INSERT value **which the fold itself then overwrote**. Measured on the fixture before D76: a class created at the root and moved once could not be placed at any instant before the move. A finding rather than a constraint, because classes predating D76 legitimately have no origin and a synthetic one needs a synthetic `client_event` — manufacturing evidence of a conversation nobody had | D22, D76 | ● |
| J63 | Every act a policy governed names the version that governed it, so an audit reads the decision rather than reconstructing it. The pattern is already here — a movement names the packing config that converted it (D57, D58) — and **the missing half is policy**: nothing records which `receiving_policy` accepted a lot, which is why D76 declined to make the closures temporal for an audit a replay could not have answered anyway. **Implemented by D87**, which is what makes D76's answer to question 148 true rather than aspirational: the audit reads the decision because the decision is written down. It cannot be reconstructed in three legitimate ways — the binding superseded, its effective range closed with nothing written, or the taxonomy re-parented, which D84 measured silently removing a shelf-life floor. A finding rather than NOT NULL, because a line dispositioned before D87 has none and backfilling would invent evidence | D22, D76, D87 | ● |
| J64 | Every reference to a **shared-reference** row resolves to the referencing row's own tenant or to a platform row. S51 makes the strict half a key; this half cannot be one, so it is checked as data across every such foreign key **read from the catalogue** rather than listed — a new shared-reference table is covered the day it exists, the same reason S50 reads the epoch's inputs that way | D55, D79 | ● |
| J65 | Every stored canonical quantity equals its preserved entered quantity converted by the factor the row names, across `stock_movement`, `goods_receipt_line` and `asserted_unit_content`. **Principle 5 has the writer convert and the database store the result, so nothing in the database has ever checked the arithmetic** — J57 checks that a row names the *right* config and stops there, which means a miscalibrated client writes a wrong quantity with a perfectly valid config beside it and the whole suite passes. Not hypothetical twice over: D91 made this error in SQL, and the fixture carried a claim asserting 400 base units against 40 entered in a unit whose factor is 1/1 for as long as the assertion tables had existed. Two factor sources, because the packaging arm converts through `packing_factor` and the unit arm through `unit.factor_num/factor_den`. **`asserted_unit_content` uses both**, since D93 gave a claim one raw code and two possible readings of it. `factor_unavailable` is its own finding rather than a skip: a row naming a config that cannot convert the level it also names is a row whose quantity nobody can ever verify | Principle 5, D92 | ● |
| J66 | No projection is older than the `freshness_bound` its step declares, and **no tenant is missing a run for a step entirely**. Every maintainer is a full-tenant fold that nothing on the write path may call, so a projected number is always behind the ledger by however long it has been since the scheduler ran — D25 working rather than a defect, since the ledger is the truth and `stock` is a cache of it. Until D95 nobody could say by how much and nothing said how much was too much. **Two failure modes and the second is the one that hides**: a projection that ran too long ago is visibly stale, while one that has *never* run for a tenant has no row at all and an anti-join written the obvious way passes over it in silence. A finding rather than a block, per D8: a stale cache never stops the floor | D25, D95 | ● |
| J67 | Every collapsed `asserted_unit` names a package whose SSCC **agrees with the one declared**, where both carry one. `asserted_unit_collapse` refuses a mismatch at the moment of writing, which settles it then; **`package.sscc` is a fold of `package_event`**, so a relabel recorded afterwards moves one side of a comparison already made. That is J57's shape exactly — a config editable after a movement named it — and it is why this is a job rather than the function's business. Only where both sides carry one: a pallet arriving with no readable label is D21's degradation ladder working, and a claim that declared no SSCC is still one we can receive against | D24, D96 | ● |
| J68 | `fulfilment_line.picked_quantity`, `packed_quantity` and `despatched_quantity` each equal the fold of `stock_movement` over that line, on **D99's shape rules**: picked left a storage location, packed went into a carton whose winning status is `sealed` or `despatched`, despatched has no `to` side at all — the mirror of D45's arrival test, which is what stops a pick and a despatch against one line summing to double the units that moved. None reads `reason`, which is a fixed vocabulary under D105 rather than a fold discriminator. **A correction nets against what it corrects** (D102): a correction may reverse only part of its target, so the fold subtracts what was taken back rather than dropping the whole movement — D100 dropped it, and a pick of twenty with five reversed read zero instead of fifteen. The correction row itself still never contributes directly, which is the half D100 had right: reversing a despatch is a movement *into* a sealed carton and would read as packing. `packed` reads `package.status`, a fold of `package_event`, and never `package.sealed_at`, which the application may UPDATE. **It reported this gap before D100 moved the maintainer and asserts the agreement after, without the query changing** — a check written to report a known gap and then guard its closure, which cannot be quietly satisfied by deleting the thing it watched. **Demonstrated rather than assumed**: a test writes movements without running the maintainer and asserts every arm fires. **D103**: the netting is recursive and lives in `stock_movement_effective`, the single definition its five readers share | D53, D99, D100, D102, D103 | |
| J69 | No `stock_movement` naming a fulfilment line moves an item other than the one that line commits, reached through its `order_line` — `fulfilment_line` carries no item of its own. A **finding rather than a CHECK**, and not only because it spans rows: D12's founding case is the picker who finds lot B where the plan said lot A, so refusing the write would stop the floor over exactly the disagreement this system exists to record. One of the two obligations D99 booked against the fold migration | D12, D99, D100 | ● |
| J70 | No movement naming a fulfilment line leaves a package that never received stock for that same line. That is a **pick out of package-held storage** — `stock.holder_location_id` and `holder_package_id` are exclusive by CHECK, so a pallet in a rack is storage — and `picked_quantity` counts movements that left a *location*, so it does not see one. D99 named the limit rather than papering over it and D100 reports it: the tempting patch, counting a from-package whose `fulfilment_id` is null, restores the consolidation double-count from the other side, because a batch tote is not any one line's carton either. The complete rule is recursive and unbuilt. Knowing a number is incomplete is different from it being wrong | D99, D100 | ● |
| J71 | No two active bins in one site claim the same `pick_sequence`. **A finding rather than a unique index**, for three reasons that point the same way: the real bin list has two such pairs, so uniqueness would refuse the import of true data; swapping two bins under a unique constraint has no legal intermediate state, and a column that exists to be reordered should be reorderable; and two bins at one position is a disagreement about the floor, which is the thing this system reports rather than refuses. The two pairs differ in what they mean — `K.32.01` and `K.32.02` are adjacent and were probably typed once, while `I.48.07` and `K.36.05` are two aisles apart and cannot both be reached at the same moment — and telling those apart is a person's job, not a constraint's. The practical cost is that a pick list ordered through either pair is non-deterministic | D46 | ● |
| J72 | Every length, width or height **we** recorded against a single unit names the `presentation` it was measured in. An apron folded twice is 250×180×30 and the same apron in a heap is something else, so the number without the word is unreproducible and unarguable. `POST /observations` refuses one — and a rule that lives only in a writer is a rule the *second* writer breaks: the prepack loader, a repair script and any future import all reach this table and none of them go through that check. Scoped to `each`, because a carton has one arrangement and asking for the word would be ceremony; and scoped to observations that are ours, because a counterparty's asserted each dimension is theirs to qualify and a finding we can never clear trains people to ignore the list | D138 | ● |
| J73 | Every table carrying a `tenant_id` **that `nylonite_app` can reach** forces row level security. J38 asserts this of the tables carrying an `@projection` column, and that set is narrower than it reads: `reported_stock` arrived with a tenant column, no projection column and row level security switched off, and **the whole suite stayed green** — the property nobody was checking was the one that decides whose rows a query returns. A tenant column is the declaration that rows belong to somebody; RLS is what makes the declaration true. **The privilege clause is not an allow-list.** RLS is not the only way to close a table and this schema uses the other one: `session` carries a `tenant_id` and has no policy at all, because the application holds no privilege on it and migration 70's `SECURITY DEFINER` functions are its only interface — demanding a policy there is a second lock on a welded door. Naming the exceptions would have worked and would have rotted, because a named exception stays true in the file long after it stops being true in the database. So the exemption *is* its reason, asked of Postgres at check time, and it audits itself: grant the application one column of `session` and this fires on the next run. Column privileges rather than table privileges, because this schema grants by column in several places and a table-level test reports no access while a column stands open | D18, D19 | ● |

---

## Numbering

**One number is retired.** J71 was created by D102 to report the correction chains
its folds could not represent, and D103 built the recursion that represents them, so
the check has no subject. **The number is not reused.** A number that has meant two
things is worse than one that means nothing, which is the same argument as the
collision above and the reason the new overreach rule went to J51 — which already
owned it — rather than into the vacancy.

**One collision has occurred.** D34 allocated J34 and J35 on 2026-08-03 while
D28 and D29 already held them. The earlier claim wins, so D34's two entries were
renumbered to J42 and J43. Nothing outside D34's own text cited them.

That collision is the argument for this file. It happened in a document that had
just been consolidated to stop exactly this failure for questions, four
paragraphs after writing down the rule, and it happened because the numbers were
hand-allocated in prose across four documents. Under the generation contract at
the top it would not have compiled.

**The vacuity count was wrong by two.** The counts table said 36 while 38 rows
carried the mark, from D44's three entries being added without it. Corrected to
41 when D47 added three more. The count is derived from the rows, so this is
another thing the generation contract at the top of this file removes rather
than fixes.

**One invariant had no number.** D31's assertion that every declared retention
floor is actually covered was stated in the decision and never entered any
register. It is S34.

**Three entries could not be stated from one document.** J3 was annotated in one
place and defined in another, J6 was extended twice in two documents, J19 was
widened in one and scoped in a second, and S2 was corrected in a third. Each
appears here as its current whole text.

## Counts

| | |
|---|---|
| Structural | 56 |
| Job-asserted | 73 |
| **Total** | **129** |
| Marked for vacuity | 71 |
| `specified` | 22 |
| `implemented` | 107 |

Seventy-one of a hundred and twenty-nine assert an absence and pass on an empty population.
That is the number worth watching, because those are the entries that will report
success on the day they stop being checked.

**Six properties are tests rather than checks**, because observing them means running a maintainer: J6's and J46's replay halves, and D68's — that a rebuild which changes nothing writes nothing. The last was written before its fix and failed at 39,589 rows, which is the only way to know a test can fail. D71 adds three more, which measure contention rather than assert it: a row that is a lock on purpose cannot be reasoned about from one connection. **D75 adds four of a different kind**, which check this register against itself, against the Rust that implements it, and against the numbers quoted outward — the counts below are derived by a test now rather than typed, after five of them drifted and the sixth was found by the test on its first run.

**The third failure mode now has a mechanism.** D49 named three: the check was
wrong, the check examined nothing, and the check's stated precondition has since
been met while it goes on reading as deliberate. Migration 12 fixed the third for
S5 and swept nothing else, and by migration 14 **fourteen** entries across both
classes were waiting on things already built — `observation` since migration 7,
`policy_binding` since migration 8, `party` and `stock` since migration 2.

D52 settled it. A blocker is a named object rather than a sentence, S42 asserts
every named object is still absent, and the eleven that name something outside
the database are counted and reported rather than quietly trusted. **Five of the
fourteen could simply run**, and running them found a platform-shipped policy
binding scoped to one tenant's item and two value versions disagreeing with their
own change records. The checks were not merely unwritten; what they guard had
already drifted.
