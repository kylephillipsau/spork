[package]
name = "nylonite-invariants"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
description = "The rules the design must always satisfy, as a suite rather than a document."

[dependencies]
nylonite-policy.workspace = true
postgres.workspace = true


[dev-dependencies]
nylonite-server = { path = "../server" }
uuid = { version = "1" }
